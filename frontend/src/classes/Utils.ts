enum Platform {
    RASPBERRY = 'raspberry',
    ARDUINO = 'arduino',
    ESP = 'esp',
    OTHER = 'other'
}

enum ImplementationType {
    TEMPLATE = 'template',
    CODE = 'code',
}

enum Topic {
    SENSOR = 'sensor',
    ACTUATOR = 'actuator',
    ROBOTICS = 'robotics',
    OTHER = 'other'
}

enum Complexity {
    SIMPLE = 'simple',
    MIDDLE = 'middle',
    HARD = 'hard'
}